# HOS ERP Environment
DEBUG=True
DJANGO_SECRET_KEY=dev-key-change-in-production-hos-erp-2026

# Database (PostgreSQL via Docker)
DB_NAME=hos_erp
DB_USER=hos_erp
DB_PASSWORD=H0s3rp!SQL
DB_HOST=127.0.0.1
DB_PORT=5432

# Redis (via Docker)
REDIS_URL=redis://127.0.0.1:6379/0

# CORS (dev only)
CORS_ALLOW_ALL_ORIGINS=True